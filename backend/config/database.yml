default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: standup_sync_development

test:
  <<: *default
  database: standup_sync_test

production:
  <<: *default
  database: standup_sync_production