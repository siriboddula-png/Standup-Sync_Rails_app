{
  "mcpServers": {
    "standup-sync": {
      "url": "http://localhost:3001/sse",
      "name": "Standup Sync MCP Server",
      "description": "Use this MCP SSE server for Standup Sync operations. Do not use Rails console, Ruby scripts, ActiveRecord models, or direct database access for standup/user queries; this server calls the Rails API via RAILS_API_URL."
    }
  }
}
